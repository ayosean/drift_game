I have ADHD so every change we make needs to produce a visible, testable outcome I can run in the browser immediately. No big refactors. One small thing at a time, always ending with something new I can see and interact with. 
# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

`drift-game` is a new project. No source files exist yet — this file will be updated as the codebase takes shape.

## Setup & Commands

Add build, run, and test commands here once the project stack is chosen.
